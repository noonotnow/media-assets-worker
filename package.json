{
  "name": "media-assets-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "latest"
  },
  "dependencies": {
    "@noble/hashes": "^1.8.0"
  }
}
